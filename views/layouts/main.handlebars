<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Password Manager</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

  <style>
    .account-card {
      margin-bottom: 10px;
      /* Add spacing between account cards */
    }

    #password-info {
      display: none;
      /* Initially hide the password info section */
      padding: 10px;
      border: 1px solid #ddd;
      margin-top: 10px;
    }
  </style>
  <link rel="stylesheet" href="/css/jass.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">My Password Manager</a>


    <nav class="d-flex">
      <!-- Conditionally render login or logout links -->
      {{#if logged_in}}
      <a href="/profile">profile</a> |
      <button class="btn btn-danger" id="logout">logout</button>
      {{else}}
      <a href="/login">login</a>
      <a href="/signup">Sign Up</a>
      {{/if}}
    </nav>
  </nav>

  <main class="container container-fluid mt-5">
    <!-- Render the sub layout -->
    {{{ body }}}
  </main>
  <footer>
    <p></p>
  </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-एँwfwqQ8hCxbwZQyzYBL4TQAeV9LM+gocvx3mE4GTMJrraoQmJKP4+2QvU5mvOe" crossorigin="anonymous"></script>

</body>

</html>